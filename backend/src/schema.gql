# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  access_token: String!
  user: User!
}

type CheckinLog {
  action: String!
  event: Event!
  eventId: ID!
  id: ID!
  location: String
  notes: String
  timestamp: DateTime!
  type: CheckinType!
  user: User!
  userId: ID!
}

enum CheckinType {
  CHECKIN
  CHECKOUT
}

type Club {
  createdAt: DateTime!
  description: String
  id: ID!
  isActive: Boolean!
  name: String!
  updatedAt: DateTime!
}

input CreateEventInput {
  clubId: ID!
  description: String
  endTime: String!
  location: String
  maxCapacity: Float
  name: String!
  startTime: String!
}

type Customer {
  address: String
  city: String
  country: String
  createdAt: DateTime!
  dateOfBirth: DateTime
  email: String!
  emergencyContactName: String
  emergencyContactPhone: String
  firstName: String!
  gender: String
  id: ID!
  isActive: Boolean!
  lastName: String!
  marketingOptIn: Boolean!
  phone: String
  postalCode: String
  state: String
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Event {
  clubId: ID!
  createdAt: String!
  description: String
  endTime: String!
  id: ID!
  isActive: Boolean!
  location: String
  maxCapacity: Float
  name: String!
  startTime: String!
  updatedAt: String!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  cancelSubscription(id: ID!): String!
  checkin(eventId: String!, qrCodeId: String!): CheckinLog!
  checkout(eventId: String!, qrCodeId: String!): CheckinLog!
  createClub(contactEmail: String!, description: String, name: String!, subdomain: String!): Club!
  createEvent(input: CreateEventInput!): String!
  createSubscription(clubId: ID!, duration: Float!, name: String!, packageId: ID, price: Float!, type: String!): Subscription!
  createSubscriptionFromPackage(packageId: ID!, userId: ID!): String!
  createSubscriptionPackage(clubId: String!, description: String, discountPrice: Float, duration: Float!, features: [String!], isPopular: Boolean, maxCheckins: Float, name: String!, price: Float!, sortOrder: Float, type: String!): String!
  deleteEvent(id: String!): String!
  deleteSubscriptionPackage(id: String!): String!
  generateQRCode(subscriptionId: ID!): String!
  generateUserQRCode(userId: ID!): QRCode!
  login(input: LoginInput!): AuthResponse!
  logout: String!
  reactivateEvent(id: String!): String!
  reactivateSubscription(id: ID!): String!
  register(input: RegisterInput!): User!
  removeClub(id: ID!): Club!
  removeEvent(id: String!): String!
  toggleSubscriptionPackageStatus(id: String!): String!
  updateClub(description: String, id: ID!, isActive: Boolean, name: String): Club!
  updateEvent(id: String!, input: UpdateEventInput!): String!
  updateSubscriptionPackage(description: String, discountPrice: Float, duration: Float, features: [String!], id: String!, isActive: Boolean, isPopular: Boolean, maxCheckins: Float, name: String, price: Float, sortOrder: Float, type: String): String!
}

type QRCode {
  createdAt: DateTime!
  expiresAt: DateTime
  id: ID!
  isActive: Boolean!
  qrCode: String!
  updatedAt: DateTime!
}

type Query {
  allSubscriptionPackages(includeInactive: Boolean): String!
  checkinLogs(clubId: String, customerId: String, eventId: String, limit: Int, offset: Int): [CheckinLog!]!
  club(id: ID!): Club!
  clubs: [Club!]!
  event(id: String!): String!
  events: String!
  me: User!
  popularSubscriptionPackages(limit: Float): String!
  profile: User!
  subscription(id: ID!): Subscription!
  subscriptionPackage(id: String!): String!
  subscriptionPackagesByClub(clubId: String!, includeInactive: Boolean): String!
  user(id: ID!): User!
  userCheckinLogs(limit: Int, offset: Int, userId: ID): [CheckinLog!]!
  userSubscriptions(userId: ID): String!
  users: [User!]!
}

input RegisterInput {
  email: String!
  firstName: String!
  lastName: String!
  password: String!
  username: String!
}

enum Role {
  CLUB_ADMIN
  CLUB_STAFF
  CUSTOMER
  SYSTEM_ADMIN
}

type Subscription {
  club: Club!
  clubId: ID!
  createdAt: DateTime!
  customer: Customer!
  customerId: ID!
  duration: Int!
  endDate: DateTime!
  id: ID!
  isActive: Boolean!
  maxCheckins: Int
  name: String!
  package: SubscriptionPackage
  price: Float!
  startDate: DateTime!
  type: SubscriptionType!
  updatedAt: DateTime!
}

type SubscriptionPackage {
  club: Club!
  createdAt: DateTime!
  description: String
  discountPrice: Float
  duration: Int!
  features: [String!]!
  id: ID!
  isActive: Boolean!
  isPopular: Boolean!
  maxCheckins: Int
  name: String!
  price: Float!
  sortOrder: Int!
  type: SubscriptionType!
  updatedAt: DateTime!
}

enum SubscriptionType {
  DAILY
  EVENT_SPECIFIC
  MONTHLY
  WEEKLY
  YEARLY
}

input UpdateEventInput {
  clubId: ID
  description: String
  endTime: String
  isActive: Boolean
  location: String
  maxCapacity: Float
  name: String
  startTime: String
}

type User {
  createdAt: DateTime!
  email: String!
  firstName: String!
  id: ID!
  isActive: Boolean!
  lastName: String!
  role: Role!
  updatedAt: DateTime!
  username: String!
}