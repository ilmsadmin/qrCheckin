export enum Role {
  ADMIN = 'ADMIN',
  STAFF = 'STAFF',
  USER = 'USER',
}

export enum SubscriptionType {
  DAILY = 'DAILY',
  WEEKLY = 'WEEKLY',
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY',
  EVENT_SPECIFIC = 'EVENT_SPECIFIC',
}

export enum CheckinType {
  CHECKIN = 'CHECKIN',
  CHECKOUT = 'CHECKOUT',
}